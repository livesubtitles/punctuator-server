commands:
  create_wsgi_home_dir:
    command: if [ ! -d "/home/wsgi" ]; then echo "Creating directory /home/wsgi"; mkdir -p /home/wsgi; chown wsgi:wsgi /home/wsgi; else echo "Directory /home/wsgi already exists, all good!"; fi
